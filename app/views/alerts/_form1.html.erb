<%= form_for([@following, @alert]) do |f| %>
  <% if @alert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alert.errors.count, "error") %> prohibited this alert from being saved:</h2>
      <ul>
        <% @alert.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :price_above %><br />
    <%= f.text_field :price_above, :value => @coin.last_price %>
  </div>
  <div class="field">
    <%= f.label :price_below %><br />
    <%= f.text_field :price_below, :value => @coin.last_price %>
  </div>
  <div class="field">
    <%= f.label :percent %><br />
    <%= f.text_field :percent %>
  </div>
  <div class="actions modal-close submit-alert">
    <%= f.submit :class => 'btn cyan accent-4' %>
  </div>
<% end %>

<!-- FORM FOR A NEW ALERT IN COINS SHOW PAGE -->
